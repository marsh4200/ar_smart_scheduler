{
  "config": {
    "step": {
      "user": {
        "title": "Create Scheduler",
        "description": "Select entities, choose time or sunrise/sunset triggers, and set the action profile.",
        "data": {
          "name": "Name",
          "target_entity": "Entities to control",
          "start_time": "Start time (used when trigger = time)",
          "start_trigger": "Start trigger",
          "start_offset": "Start offset (minutes)",
          "end_time": "End time (used when trigger = time)",
          "end_trigger": "End trigger",
          "end_offset": "End offset (minutes)",
          "weekdays": "Weekdays",
          "second_enabled": "Enable second window",
          "second_start_time": "Second start time (used when trigger = time)",
          "second_start_trigger": "Second start trigger",
          "second_start_offset": "Second start offset (minutes)",
          "second_end_time": "Second end time (used when trigger = time)",
          "second_end_trigger": "Second end trigger",
          "second_end_offset": "Second end offset (minutes)"
        }
      }
    }
  }
}
